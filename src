/* Generated By:JavaCC: Do not edit this line. MiniLangParser.java */
import java.util.*;
import java.io.*;

public class MiniLangParser implements MiniLangParserConstants {

    public static ASTNode.Program parseString(String source) throws ParseException {
        MiniLangParser parser = new MiniLangParser(new StringReader(source));
        return parser.Program();
    }

    public static ASTNode.Program parseFile(String path)
            throws ParseException, FileNotFoundException {
        MiniLangParser parser = new MiniLangParser(new FileInputStream(path));
        return parser.Program();
    }

  final public ASTNode.Program Program() throws ParseException {
    Token name;
    List body;
    jj_consume_token(INTEGER);
    name = jj_consume_token(ID);
    jj_consume_token(LPAREN);
    jj_consume_token(RPAREN);
    jj_consume_token(LBRACE);
    body = Statements();
    jj_consume_token(RBRACE);
    jj_consume_token(0);
      {if (true) return new ASTNode.Program("integer", name.image, body);}
    throw new Error("Missing return statement in function");
  }

  final public List Statements() throws ParseException {
    List stmts = new ArrayList();
    ASTNode s;
    label_1:
    while (true) {
      s = Statement();
                        stmts.add(s);
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case RETURN:
      case IF:
      case WHILE:
      case ID:
        ;
        break;
      default:
        jj_la1[0] = jj_gen;
        break label_1;
      }
    }
      {if (true) return stmts;}
    throw new Error("Missing return statement in function");
  }

  final public ASTNode Statement() throws ParseException {
    ASTNode s;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case ID:
      s = AssignStmt();
      break;
    case RETURN:
      s = ReturnStmt();
      break;
    case IF:
      s = IfStmt();
      break;
    case WHILE:
      s = WhileStmt();
      break;
    default:
      jj_la1[1] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
      {if (true) return s;}
    throw new Error("Missing return statement in function");
  }

  final public ASTNode AssignStmt() throws ParseException {
    Token id;
    ASTNode expr;
    id = jj_consume_token(ID);
    jj_consume_token(ASSIGN);
    expr = Exp();
    jj_consume_token(SEMICOL);
      {if (true) return new ASTNode.AssignStmt(id.image, expr, id.beginLine);}
    throw new Error("Missing return statement in function");
  }

  final public ASTNode ReturnStmt() throws ParseException {
    Token ret;
    ASTNode expr;
    ret = jj_consume_token(RETURN);
    expr = Exp();
    jj_consume_token(SEMICOL);
      {if (true) return new ASTNode.ReturnStmt(expr, ret.beginLine);}
    throw new Error("Missing return statement in function");
  }

  final public ASTNode IfStmt() throws ParseException {
    Token ifTok;
    ASTNode cond;
    List thenBody, elseBody;
    ifTok = jj_consume_token(IF);
    jj_consume_token(LPAREN);
    cond = Exp();
    jj_consume_token(RPAREN);
    jj_consume_token(LBRACE);
    thenBody = Statements();
    jj_consume_token(RBRACE);
    jj_consume_token(ELSE);
    jj_consume_token(LBRACE);
    elseBody = Statements();
    jj_consume_token(RBRACE);
      {if (true) return new ASTNode.IfStmt(cond, thenBody, elseBody, ifTok.beginLine);}
    throw new Error("Missing return statement in function");
  }

  final public ASTNode WhileStmt() throws ParseException {
    Token whTok;
    ASTNode cond;
    List body;
    whTok = jj_consume_token(WHILE);
    jj_consume_token(LPAREN);
    cond = Exp();
    jj_consume_token(RPAREN);
    jj_consume_token(LBRACE);
    body = Statements();
    jj_consume_token(RBRACE);
      {if (true) return new ASTNode.WhileStmt(cond, body, whTok.beginLine);}
    throw new Error("Missing return statement in function");
  }

  final public ASTNode Exp() throws ParseException {
    ASTNode.ValueExpr left, right;
    left = Value();
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case PLUS:
      jj_consume_token(PLUS);
      right = Value();
          {if (true) return new ASTNode.BinopExpr(left, right);}
      break;
    default:
      jj_la1[2] = jj_gen;
          {if (true) return left;}
    }
    throw new Error("Missing return statement in function");
  }

  final public ASTNode.ValueExpr Value() throws ParseException {
    Token t;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case ID:
      t = jj_consume_token(ID);
      break;
    case NUMBER:
      t = jj_consume_token(NUMBER);
      break;
    default:
      jj_la1[3] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
      {if (true) return new ASTNode.ValueExpr(t.image);}
    throw new Error("Missing return statement in function");
  }

  /** Generated Token Manager. */
  public MiniLangParserTokenManager token_source;
  SimpleCharStream jj_input_stream;
  /** Current token. */
  public Token token;
  /** Next token. */
  public Token jj_nt;
  private int jj_ntk;
  private int jj_gen;
  final private int[] jj_la1 = new int[4];
  static private int[] jj_la1_0;
  static {
      jj_la1_init_0();
   }
   private static void jj_la1_init_0() {
      jj_la1_0 = new int[] {0x1580,0x1580,0x40000,0x1800,};
   }

  /** Constructor with InputStream. */
  public MiniLangParser(java.io.InputStream stream) {
     this(stream, null);
  }
  /** Constructor with InputStream and supplied encoding */
  public MiniLangParser(java.io.InputStream stream, String encoding) {
    try { jj_input_stream = new SimpleCharStream(stream, encoding, 1, 1); } catch(java.io.UnsupportedEncodingException e) { throw new RuntimeException(e); }
    token_source = new MiniLangParserTokenManager(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 4; i++) jj_la1[i] = -1;
  }

  /** Reinitialise. */
  public void ReInit(java.io.InputStream stream) {
     ReInit(stream, null);
  }
  /** Reinitialise. */
  public void ReInit(java.io.InputStream stream, String encoding) {
    try { jj_input_stream.ReInit(stream, encoding, 1, 1); } catch(java.io.UnsupportedEncodingException e) { throw new RuntimeException(e); }
    token_source.ReInit(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 4; i++) jj_la1[i] = -1;
  }

  /** Constructor. */
  public MiniLangParser(java.io.Reader stream) {
    jj_input_stream = new SimpleCharStream(stream, 1, 1);
    token_source = new MiniLangParserTokenManager(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 4; i++) jj_la1[i] = -1;
  }

  /** Reinitialise. */
  public void ReInit(java.io.Reader stream) {
    jj_input_stream.ReInit(stream, 1, 1);
    token_source.ReInit(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 4; i++) jj_la1[i] = -1;
  }

  /** Constructor with generated Token Manager. */
  public MiniLangParser(MiniLangParserTokenManager tm) {
    token_source = tm;
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 4; i++) jj_la1[i] = -1;
  }

  /** Reinitialise. */
  public void ReInit(MiniLangParserTokenManager tm) {
    token_source = tm;
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 4; i++) jj_la1[i] = -1;
  }

  private Token jj_consume_token(int kind) throws ParseException {
    Token oldToken;
    if ((oldToken = token).next != null) token = token.next;
    else token = token.next = token_source.getNextToken();
    jj_ntk = -1;
    if (token.kind == kind) {
      jj_gen++;
      return token;
    }
    token = oldToken;
    jj_kind = kind;
    throw generateParseException();
  }


/** Get the next Token. */
  final public Token getNextToken() {
    if (token.next != null) token = token.next;
    else token = token.next = token_source.getNextToken();
    jj_ntk = -1;
    jj_gen++;
    return token;
  }

/** Get the specific Token. */
  final public Token getToken(int index) {
    Token t = token;
    for (int i = 0; i < index; i++) {
      if (t.next != null) t = t.next;
      else t = t.next = token_source.getNextToken();
    }
    return t;
  }

  private int jj_ntk() {
    if ((jj_nt=token.next) == null)
      return (jj_ntk = (token.next=token_source.getNextToken()).kind);
    else
      return (jj_ntk = jj_nt.kind);
  }

  private java.util.List jj_expentries = new java.util.ArrayList();
  private int[] jj_expentry;
  private int jj_kind = -1;

  /** Generate ParseException. */
  public ParseException generateParseException() {
    jj_expentries.clear();
    boolean[] la1tokens = new boolean[20];
    if (jj_kind >= 0) {
      la1tokens[jj_kind] = true;
      jj_kind = -1;
    }
    for (int i = 0; i < 4; i++) {
      if (jj_la1[i] == jj_gen) {
        for (int j = 0; j < 32; j++) {
          if ((jj_la1_0[i] & (1<<j)) != 0) {
            la1tokens[j] = true;
          }
        }
      }
    }
    for (int i = 0; i < 20; i++) {
      if (la1tokens[i]) {
        jj_expentry = new int[1];
        jj_expentry[0] = i;
        jj_expentries.add(jj_expentry);
      }
    }
    int[][] exptokseq = new int[jj_expentries.size()][];
    for (int i = 0; i < jj_expentries.size(); i++) {
      exptokseq[i] = (int[])jj_expentries.get(i);
    }
    return new ParseException(token, exptokseq, tokenImage);
  }

  /** Enable tracing. */
  final public void enable_tracing() {
  }

  /** Disable tracing. */
  final public void disable_tracing() {
  }

}
